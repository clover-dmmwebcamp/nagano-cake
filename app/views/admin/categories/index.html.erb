<div class="container">
    <div class="row">
        <div class="col-lg-8">
            <h5 class="mt-4 ml-5">ジャンル一覧・追加</h5>
            <div class="my-5 ml-2">
                <% if @category.errors.any? %>
                    <ul class="alert alert-danger" role="alert">
                        <h6 class="alert-heading">
                            <%= @category.errors.count %>件のエラーが発生しました
                        </h6>
                        <% @category.errors.full_messages.each do |message| %>
                            <li><%= message %></li>
                        <% end %>
                    </ul>
                <% end %>
                <%= form_with model: @category, url: admin_categories_path do |f| %>
                    <div class="mr-5 d-inline-block">
                        <%= f.label :name, "ジャンル名", class:"font-weight-bold" %>
                    </div>
                    <div class="mr-5 d-inline-block">
                        <%= f.text_field :name, placeholder:"ジャンル名" %>
                    </div>
                    <%= f.submit "新規登録", class:"btn btn-success" %>
                <% end %>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-5">
            <table class="table table-inverse ml-2">
                <thead class="font-weight-bold table-secondary">
                    <tr>
                        <th class="text-center">ジャンル</th>
                        <th colspan="3"></th>
                    </tr>
                </thead>
                <tbody>
                    <% @categories.each do |category| %>
                        <tr>
                            <td class="align-middle"><%= category.name %></td>
                            <td></td>
                            <td></td>
                            <td class="text-center">
                                <%= link_to "編集する", edit_admin_category_path(category.id), class:"btn btn-success" %>
                            </td>
                        </tr>
                    <% end %>
                </tbody>
            </table>
        </div>
    </div>
</div>