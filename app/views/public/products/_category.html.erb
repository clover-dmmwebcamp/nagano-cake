<div class="col-lg-2 mt-5 p-0">
    <div class="card border-dark">
        <h6 class="card-header text-center border-dark">ジャンル検索</h6>
        <% categories.each do |category| %>
            <li class="list-group-item border-0"><%= category.name %></li>
        <% end %>
    </div>
</div>