<div class="container">
    <h4 class="my-3 ml-4">検索結果一覧(全<%= @products.count %>件) "<%= @content %>"</h4>
  <div class="row">
    <%= render 'searches/search_result', results: @results %>
  </div>
  <div class="d-flex justify-content-center mt-3 custom-pagination">
    <%= paginate @results %>
  </div>
</div>